import { Component } from '@angular/core';

// PUBLIC_INTERFACE
@Component({
  selector: 'wavelog-log-session',
  standalone: true,
  template: `<section>
    <h2>Log New Surf Session</h2>
    <p>Log new session form goes here.</p>
  </section>`,
})
export class LogSessionComponent {}
